import type { ExtensionManifest } from '../../../types';

export const manifest: ExtensionManifest = {
  name: 'core.vanilla-logger',
  display_name: 'DOM Logger (Vanilla TS)',
  description: 'Highlights the main content area with a border when active to demonstrate vanilla DOM manipulation.',
  version: '1.0.0',
  author: 'SillyTavern Team',
};
