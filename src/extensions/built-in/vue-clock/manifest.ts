import type { ExtensionManifest } from '../../../types';

export const manifest: ExtensionManifest = {
  name: 'core.vue-clock',
  display_name: 'Sidebar Clock (Vue)',
  description: 'Adds a reactive clock widget to the sidebar using a Vue SFC.',
  version: '1.0.0',
  author: 'SillyTavern Team',
};
